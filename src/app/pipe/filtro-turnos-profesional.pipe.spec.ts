import { FiltroTurnosProfesionalPipe } from './filtro-turnos-profesional.pipe';

describe('FiltroTurnosProfesionalPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosProfesionalPipe();
    expect(pipe).toBeTruthy();
  });
});
